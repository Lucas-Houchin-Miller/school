<h1>Course #<%= @course.id %></h1>

<div>
  <strong>Name</strong>:<br />
  <p><%= @course.name %></p>
</div>
<div>
  <strong>Students</strong>:<br />
  <p><%= @course.enrollments.each do |student|
end  %>
  </p>
</div>

<a href="/courses/<%= @course.id %>/edit">Edit</a> |
<a href="/courses/<%= @course.id %>/destroy">Destroy</a>
